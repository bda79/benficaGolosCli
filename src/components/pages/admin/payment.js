import React from "react";

const Payment = () => {
    //TODO mostrar tabela pagamento no user ou
    // adicionar pagamento (nav e route) ao utilizador normal
    return (
        <div>
            <h1>Payment Page</h1>
        </div>
    );
}

export default Payment;